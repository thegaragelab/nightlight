#!/bin/sh
#-----------------------------------------------------------
# Create a 'gource' video from the repository
#-----------------------------------------------------------

PROJECT=nightlight
#gource -1280x720 -o - | ffmpeg -y -r 60 -f image2pipe -vcodec ppm -i - -vcodec libx264 -preset ultrafast -pix_fmt yuv420p -crf 1 -threads 0 -bf 0 ${PROJECT}.mp4
gource -704x480 -o - | ffmpeg -y -r 60 -f image2pipe -vcodec ppm -i - -vcodec libx264 -preset ultrafast -pix_fmt yuv420p -crf 1 -threads 0 -bf 0 ${PROJECT}.mp4
